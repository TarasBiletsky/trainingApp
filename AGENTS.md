# Project instructions

- Follow `LIBRARY_NAMING_GUIDE.md`.
- After completing and verifying project changes, commit and push your own changes to the current remote branch.
- Treat the responsive web interface as the primary product. Implement and verify UI requests in web first; defer iOS parity work and record it in `WEB_CHANGELOG.md` unless the user explicitly asks for iOS.
